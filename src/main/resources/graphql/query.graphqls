# Available graphql queries for this API
type Query {
    book(id: ID): Book
}

type Book {
    id: ID
    name: String
    genre: Genre
}

enum Genre {
    DETECTIVE
    HISTORICAL_NOVEL
    NONFICTION
}